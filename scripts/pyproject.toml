[project]
name = "scripts"
version = "0.1.0"
description = ""
authors = [{ name = "ThatDevShparki", email = "shparki@thatdev.io" }]
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["pydantic (>=2.10.6,<3.0.0)", "sfzlint (>=0.1.4,<0.2.0)"]


[tool.poetry]
package-mode = false

[tool.ruff.format]
quote-style = 'single'

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
